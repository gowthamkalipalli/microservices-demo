pipeline:
  name: gk-microservice-pipeline
  identifier: gkmicroservicepipeline
  projectIdentifier: SFTY_Training
  orgIdentifier: default
  tags:
    ai_generated: "true"
  properties:
    ci:
      codebase:
        connectorRef: gkmicroprojectgittoken
        repoName: microservices-demo
        build:
          type: branch
          spec:
            branch: main
  stages:
    - stage:
        name: CI
        identifier: CI
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: false
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec:
              size: xxxlarge
              imageSpec:
                imageName: ubuntu-24
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    shell: Sh
                    command: |-
                      cd src/${SERVICE_NAME}

                      if [ -f "go.mod" ]; then
                        echo "Go service detected"
                        go build -o app
                      elif [ -f "pom.xml" ]; then
                        echo "Java service detected"
                        mvn clean package -DskipTests
                      elif [ -f "package.json" ]; then
                        echo "Node service detected"
                        npm install && npm run build
                      fi
                  timeout: 10m
            rollbackSteps: []
  description: This pipeline was updated by Harness AI on 2026-01-29 12:23:36 UTC by Gowtham.
